    <header>
        <nav>
            <ul>
                <li><a href="/#Cabecera">Inicio</a></li>
                <li><a href="/#Proyectos">Proyectos</a></li>
                <li><a href="/#Experiencia">Experiencia</a></li>
                <li><a href="/#Habilidades">Habilidades</a></li>
            </ul>
        </nav>
    </header>
    <style>
        header {
          width: 100%;
          position: fixed;
          left: 50%;
          top: 0px;
          bottom: auto;
          transform: translateX(-50%);
          margin: 16px 0px;
          grid-area: header;
          margin-top: 16px;
        }
      
        nav {
          width: fit-content;
          margin: 0px auto 0px auto;
          background-color: #2c3e50;
          border-radius: 16px;
    
        }
        nav a {
    color: white;
    text-decoration: none;
    padding: 10px 20px;
    border-radius: 5px;
    transition: background-color 0.3s, color 0.3s;
  }

  nav a:hover {
    background-color: rgb(75, 74, 105);
    color: #ffffff; /* Color del texto al pasar el ratón */
  }
        ul {
          display: flex;
          justify-content: space-between;
          flex-direction: row;
          align-items: stretch;
          margin: 0;
          padding: 0;
          border-radius: 32px;
          border: var(--card-border);
          background-color: var(--gray-medium-transparent);
          box-shadow: var(--box-shadow);
          backdrop-filter: var(--blur);
          -webkit-backdrop-filter: var(--blur);
        }
      
        li {
          list-style-type: none;
          margin: 8px;
          padding: 8px 0;
        }
      
        div {
          display: none;
        }
      
        a {
          text-decoration: none;
          color: #ffffff;
          padding: 8px 16px;
          border-radius: 24px;
        }
      
        a:hover {
          background-color: var(--gray-ultra-light-transparent);
          backdrop-filter: var(--blur);
          -webkit-backdrop-filter: var(--blur);
          text-decoration: none;
          color: white;
        }
      </style>